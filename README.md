# Python Weather App 

A command-line Python application to get real-time weather details using OpenWeatherMap API.

## Features
- Search weather for a single city.
- Fetch weather for multiple cities together.
- Shows Temperature (°C), Humidity (%), and Weather condition.

## How to Run

1. Clone or download this project.
2. Install dependencies:
   ```bash
   pip install requests
